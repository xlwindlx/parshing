<p id="notice"><%= notice %></p>

<h2 style="text-align:center;">전공과목 시간표</h2>

<table>
  <thead>
    <tr>
      <th>이수구분</th>
      <th>과목명</th>
      <th>학년</th>
      <th>학점</th>
      <th>전공명</th>
      <th>교강사</th>
      <th>요일/시간</th>
      <th>강의실</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @profs.each do |prof| %>
      <tr>
        <td><%= prof.div %></td>
        <td><%= prof.title %></td>
        <td style="text-align:center;"><%= prof.grade %></td>
        <td style="text-align:center;"><%= prof.grades %></td>
        <td style="text-align:center;"><%= prof.subject %></td>
        <td style="text-align:center;"><%= prof.proffesion %></td>
        <td><%= prof.time %></td>
        <td style="text-align:center;"><%= prof.classroom %></td>
        <td><%= link_to '자세히 보기', prof %></td>
        <td><%= link_to '수정', edit_prof_path(prof) %></td>
        <td><%= link_to '삭제', prof, method: :delete, data: { confirm: '삭제할까요?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to '추가하기', new_prof_path %>
