<p id="notice"><%= notice %></p>

<h2 style="text-align:center;">전공과목 시간표</h2>

<table>
  <caption><%= link_to '뒤로가기', home_index_path %></caption>
  <thead>
    <tr>
      <th>이수 구분</th>
      <th>과목명</th>
      <th>학년</th>
      <th>학점</th>
      <th>전공명</th>
      <th>교강사</th>
      <th>요일/시간</th>
      <th>강의실</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @profs.each do |prof| %>
      <tr>
        <td><%= prof.div %></td>
        <td><%= prof.title %></td>
        <td><%= prof.grade %></td>
        <td><%= prof.grades %></td>
        <td><%= prof.subject %></td>
        <td><%= prof.proffesion %></td>
        <td><%= simple_format prof.time %></td>
        <td><%= prof.classroom %></td>
        <td><%= link_to '자세히 보기', prof %></td>
        <td><%= link_to '수정', edit_prof_path(prof) %></td>
        <td><%= link_to '삭제', prof, method: :delete, data: { confirm: '삭제할까요?' } %></td>
      </tr>
    <% end %>
    <tr>
      <td colspan="11" style="text-align:center;">
        <h2><strong><%= link_to '과목 추가하기', new_normal_path %></strong></h2>
      </td>
    </tr>
  </tbody>
</table>

<%= link_to '뒤로가기', home_index_path %>